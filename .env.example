FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
SESSION_SECRET=
ADMIN_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: free Upstash Redis serves the leaderboards so they keep
# working when Firebase hits its daily free quota. Both values are on the
# Upstash console page for your database, under "REST API".
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
